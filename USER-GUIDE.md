# คู่มือใช้งานระบบ DemeterRich Ops

> เอกสารนี้สำหรับ**ทีมที่ใช้งานจริง** (ไม่ใช่คู่มือ deploy — ดู README.md สำหรับเรื่องทางเทคนิค)
> อัปเดตล่าสุด: 2026-07-21

---

## ภาพรวม

ระบบเครื่องมือภายในทีม DemeterRich รองรับ MAXIMUS เป็นสินค้าตัวแรก ออกแบบให้ขยายไปสินค้าอื่นได้ในอนาคต

**เข้าใช้งานที่:** https://demeterrich-ops.web.app (ต้อง login — แอดมินเป็นคนเพิ่มบัญชีให้เท่านั้น ไม่มีสมัครเอง)

**หลักการที่ยึดตลอดทั้งระบบ:** ไม่มีจุดไหนที่ "ตัดสินใจ" หรือ "ส่งอะไรออกไปหาลูกค้า" ให้เองโดยไม่มีคนกดยืนยัน — ทุกเครื่องมือเป็นตัวช่วยให้ทีมทำงานเร็วขึ้น ไม่ใช่ตัวแทนที่ทำงานแทนคน

---

## 1. 📦 ออเดอร์ & CPA Tracker — `orders.html`

**ใช้ตอนไหน:** ทุกครั้งที่มีลูกค้าสั่งซื้อจริง (หลังตกลงในแชทแล้ว) และทุกวันที่ยิงแอด

### ✅ ทำได้
- บันทึกออเดอร์ (ชื่อ, แพ็กเกจ, ราคา, ที่อยู่, ช่องทางที่มา)
- บันทึกค่าแอดรายวัน
- คำนวณ CPA (ต้นทุนต่อออเดอร์) อัตโนมัติ เทียบกับเพดาน 385 บาทที่ตั้งไว้ พร้อมบอกว่าอยู่ในเกณฑ์ดีหรือแย่
- ดูยอดขาย/จำนวนออเดอร์สะสมของเดือนนี้แบบเรียลไทม์

### 🚫 ทำไม่ได้
- **ไม่เชื่อมกับระบบรับเงินจริง** — ต้องกรอกออเดอร์เองหลังลูกค้าโอนเงินและยืนยันแล้วเท่านั้น ไม่ใช่ระบบตรวจสลิปอัตโนมัติ
- **ไม่ดึงค่าแอดจาก Facebook Ads Manager อัตโนมัติ** — ต้องเข้าไปดูตัวเลขจริงใน Ads Manager แล้วมาพิมพ์ใส่เองทุกวัน
- ไม่มีการแจ้งเตือน (เช่น แจ้งเตือนเมื่อ CPA เกินเพดาน) — ต้องเข้ามาดูเอง
- ไม่รองรับหลายสกุลเงิน (บาทเท่านั้น)

---

## 2. 📨 กล่องข้อความ — `inbox.html`

**ใช้ตอนไหน:** ตอบแชทลูกค้าที่ทักเข้ามาทาง Facebook Messenger

### ✅ ทำได้
- รับข้อความจากลูกค้าเข้ามาแสดงแบบเรียลไทม์ (เมื่อตั้งค่า Facebook เสร็จแล้ว — ดูด้านล่าง)
- แนะนำสคริปต์คำตอบที่ใกล้เคียงที่สุดให้อัตโนมัติ กดเลือกแล้วแก้ไขก่อนส่งได้
- เตือนชัดเจนถ้าคำถามเข้าข่าย "ต้องส่งต่อ Roger" (เช่น ถามเรื่องทะเบียน/ความปลอดภัยสัตว์เลี้ยง) — ไม่ให้กดส่งด่วน

### 🚫 ทำไม่ได้ (สำคัญที่สุด — อ่านให้ครบ)
- **ไม่ตอบลูกค้าอัตโนมัติเด็ดขาดในทุกกรณี** ต่อให้ระบบแนะนำคำตอบมาให้ ก็ต้องมีคนกดปุ่ม "ส่ง" เองเสมอ ไม่มีการส่งเองระหว่างที่ไม่มีคนอยู่หน้าจอ
- **ยังใช้งานจริงไม่ได้ตอนนี้** จนกว่าจะมี: เพจ Facebook ของ MAXIMUS + Meta App + Page Access Token (ขั้นตอนอยู่ใน README.md หัวข้อ "Meta App Setup Checklist")
- รับได้แค่ข้อความตัวหนังสือเท่านั้น — รูปภาพ/สติกเกอร์/ไฟล์แนบที่ลูกค้าส่งมาจะไม่ถูกบันทึก (เห็นแค่ในแอป Messenger จริงเท่านั้น ต้องเช็กคู่กัน)
- ไม่มีระบบมอบหมายงาน (ใครตอบคนไหน) — ถ้ามีแอดมินหลายคน ต้องตกลงกันเองว่าใครดูแลช่วงไหน

---

## 3. 🗓 ปฏิทินคอนเทนต์ — `content.html`

**ใช้ตอนไหน:** ติดตามว่าโพสต์คอนเทนต์ครบตามแผน 30 วันหรือยัง

### ✅ ทำได้
- ติ๊กสถานะว่าโพสต์แต่ละวันโพสต์แล้วหรือยัง
- เช็กอัตโนมัติว่าครบ 12 โพสต์บังคับก่อนยิงแอดหรือยัง (ตามกติกา page-setup.md)

### 🚫 ทำไม่ได้
- **ไม่โพสต์ขึ้นเพจ Facebook ให้อัตโนมัติ** — ติ๊กในนี้แค่บันทึกสถานะ ต้องไปโพสต์เองที่เพจจริงต่างหาก
- **ไม่สร้างรูปภาพให้** — ต้องเอา prompt จาก `marketing/image-prompts.md` (ในโปรเจกต์ MAXIMUS) ไปสร้างเองผ่าน Nano Banana หรือ Canva
- ยังมีแค่คอนเทนต์ของ MAXIMUS เท่านั้น (สินค้าใหม่ต้องเพิ่มปฏิทินเองตามที่ README อธิบาย)

---

## 4. 💬 คลังสคริปต์แชท — `chat-reference.html`

**ใช้ตอนไหน:** ค้นหาคำตอบสำเร็จรูปตอนตอบลูกค้า (ใช้คู่กับ inbox.html หรือใช้แยกก็ได้ถ้าตอบผ่าน Messenger โดยตรง)

### ✅ ทำได้
- ค้นหาด้วยคำสำคัญ (เช่น "แพง", "สัตว์เลี้ยง", "คืนเงิน") แล้วเจอสคริปต์ทันที
- กดคัดลอกไปวางที่อื่นได้เลย

### 🚫 ทำไม่ได้
- ไม่ auto-fill เข้า Messenger โดยตรง (ต้อง copy-paste เอง ถ้าไม่ได้ใช้ผ่าน inbox.html)
- ถ้าเจอคำถามที่ไม่มีในคลัง ระบบจะไม่แต่งคำตอบให้ — ต้องคิดเองหรือส่งต่อ Roger

---

## 5. 🛡 เช็กคำต้องห้าม — `compliance-check.html`

**ใช้ตอนไหน:** ก่อนโพสต์ ad copy หรือคอนเทนต์ใดๆ ที่มีการเคลมสรรพคุณ

### ✅ ทำได้
- สแกนข้อความหาคำต้องห้าม/คำเสี่ยงที่ตั้งไว้ล่วงหน้า (เช่น "ORGANIC 100%", "รับประกัน") แบบเรียลไทม์ขณะพิมพ์

### 🚫 ทำไม่ได้
- **ไม่ใช่การตรวจสอบทางกฎหมายที่สมบูรณ์** — เช็กได้แค่คำที่ตรงรูปแบบที่ตั้งไว้ล่วงหน้าเท่านั้น ไม่เข้าใจบริบทหรือความหมายแฝง
- **ไม่ทดแทนการตรวจสอบจริงก่อนเผยแพร่** — เนื้อหาที่สำคัญ (ad copy ชุดแรก, เนื้อหาที่แตะ compliance) ควรให้ Roger หรือคนที่เข้าใจ `compliance/approved-claims.md` ทวนอีกรอบเสมอ

---

## 6. 🎨 ตกแต่ง Sale Page — `salepage-settings.html`

**ใช้ตอนไหน:** ปรับหน้าตาเว็บขายตามเทศกาล/แคมเปญ

### ✅ ทำได้
- เปลี่ยนสี 3 จุดหลัก (พื้นเข้ม/สีเน้น/พื้นอ่อน), ฟอนต์หัวข้อ+ไทย, รูป header, ข้อความ hero (eyebrow/headline/subheadline)
- **มีผลทันทีที่หน้าเว็บจริงโดยไม่ต้อง deploy ใหม่** (บันทึกแล้วเห็นผลเลย)

### 🚫 ทำไม่ได้
- แก้ได้แค่ส่วน **Hero (บนสุดของหน้า)** เท่านั้น — ส่วนอื่น (ตารางราคา, FAQ, นโยบายคืนสินค้า, ตารางการใช้งาน) แก้ผ่านหน้านี้ไม่ได้ ต้องแก้โค้ดโดยตรง (แจ้ง Roger/Claude ให้ช่วยแก้)
- ไม่มีปุ่ม "ย้อนกลับ" อัตโนมัติ — ถ้าตั้งค่าใหม่แล้วไม่ชอบ ต้องจำค่าตั้งต้นเอง หรือกด "รีเซ็ตเป็นค่าเริ่มต้น" เพื่อกลับไปดีไซน์ในโค้ด (จะไม่ใช่ค่าที่ตั้งไว้ก่อนหน้า)

---

## หน้าขายจริง — https://maximus-salepage.web.app

**นี่คือหน้าที่ลูกค้าเห็นจริง** (ไม่ต้อง login, ทุกคนเข้าได้)

### ✅ ทำได้
- แสดงข้อมูลสินค้า ราคา วิธีใช้ นโยบายคืนสินค้า
- ปุ่ม CTA พาไปแชท Messenger เพื่อสั่งซื้อ
- รับการปรับแต่งจาก control panel แบบเรียลไทม์

### 🚫 ทำไม่ได้ (ตั้งใจออกแบบแบบนี้)
- **ไม่มีระบบ checkout/ใส่บัตรเครดิตในหน้านี้เลย** — การสั่งซื้อและชำระเงินทั้งหมดเกิดที่แชทเท่านั้น เป็นการตัดสินใจร่วมกันตั้งแต่ต้นเพื่อให้มีคนตรวจออเดอร์ก่อนทุกครั้ง (ดู `sales/order-sop.md`)
- ไม่เก็บข้อมูลลูกค้า (ไม่มีฟอร์มกรอกอะไรในหน้านี้)

---

## ภาพรวม: สิ่งที่ทั้งระบบ "ไม่ทำ" โดยตั้งใจ (ไม่ใช่ข้อจำกัดทางเทคนิค แต่เป็นการตัดสินใจ)

| สิ่งที่ไม่ทำ | เหตุผล |
|---|---|
| ส่งข้อความหาลูกค้าอัตโนมัติ | ป้องกันคำตอบผิด/เคลมเกินจริงหลุดไปหาลูกค้าโดยไม่มีคนเห็นก่อน — โดยเฉพาะกลุ่มไม้ด่างที่ตรวจสอบเก่งมาก |
| ประมวลผลการชำระเงิน/บัตรเครดิต | ให้มีจุดตรวจสอบจากคนเสมอก่อนยืนยันออเดอร์ |
| ยิง/ปรับงบโฆษณาอัตโนมัติ | การตัดสินใจเรื่องงบต้องอิงกับ CPA จริงและดุลยพินิจของทีม ไม่ใช่สูตรตายตัว |
| โพสต์คอนเทนต์ขึ้น Facebook อัตโนมัติ | ต้องมีคนเห็นก่อนเผยแพร่เสมอ |
| ตั้งราคา/นโยบายเอง | เป็นการตัดสินใจทางธุรกิจที่ต้องเป็นคนตัดสิน ระบบแค่ช่วยคำนวณ/แนะนำ |

---

## ถ้าระบบล่มหรือเข้าไม่ได้ (Manual Fallback)

- **ออเดอร์:** จดใส่ Google Sheet ชั่วคราว แล้วค่อยกรอกเข้าระบบทีหลัง
- **ตอบแชท:** เปิดไฟล์ `sales/chat-playbook.md` และ `sales/objection-bank.md` ตรงๆ (อยู่ในโปรเจกต์ MAXIMUS)
- **เช็กคำต้องห้าม:** เทียบกับตาราง 🚫/✅/⚠️ ใน `compliance/approved-claims.md` ด้วยตาเปล่า
- **ปฏิทินคอนเทนต์:** เปิด `marketing/content-calendar.md` ตรงๆ

ระบบนี้คือ**ตัวช่วยให้เร็วขึ้น** ไม่ใช่จุดเดียวที่พังแล้วธุรกิจหยุด — ทุกอย่างทำด้วยมือได้เสมอ
